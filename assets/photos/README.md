# Photography assets

Put your selected photography files here. Recommended export:
- JPEG or WebP
- 1800–2400 px long edge
- sRGB
- quality around 80–90

Once photos are selected, replace the placeholder blocks in `photography.html` with `<figure>` elements.
